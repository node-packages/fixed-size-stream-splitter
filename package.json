{
  "name": "fixed-size-stream-splitter",
  "version": "1.0.0",
  "description": "split a stream into many fixed-size streams",
  "main": "index.js",
  "scripts": {
    "test": "tape test/*.js"
  },
  "author": "substack",
  "license": "MIT",
  "dependencies": {
    "inherits": "^2.0.1",
    "readable-stream": "^2.0.2"
  },
  "devDependencies": {
    "concat-stream": "^1.5.0",
    "tape": "^4.2.0"
  }
}
